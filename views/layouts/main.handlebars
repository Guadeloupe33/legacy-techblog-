<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Add Bootstrap CSS and JavaScript links here -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/js/bootstrap.min.js"></script>
    <!-- Add your custom CSS link here -->
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>MVC Tech Blog</title>
</head>

<body>
    <div class="container-fluid">
        <header class="hero bg-dark text-white">
            <div class="d-flex justify-content-between align-items-center">
                <nav class="nav">
                    <a class="link text-white" href="/">Home</a>
                    <a class="link text-white" href="/dashboard">Dashboard</a>
                    {{#if loggedIn}}
                    <a class="link text-white" href="/" id="logout">Logout</a>
                    {{else}}
                    <a class="link text-white" href="/login">Login</a>
                    {{/if}}
                </nav>
                <h1 class="app-title">The Tech Blog</h1>
            </div>
        </header>

        <main class="mt-4">
            {{{ body }}}
        </main>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

    <!-- Bootstrap Modal -->
    <div class="modal fade" id="instructionModal" tabindex="-1" role="dialog" aria-labelledby="instructionModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header bg-dark text-white">
                    <h5 class="modal-title" id="instructionModalLabel">Instructions</h5>
                    <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Please sign up and then click on the new post button to create a new post.  You can then logout and log back in with the username and password that you have created.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Got it</button>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
